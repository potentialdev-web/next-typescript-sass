export interface JobType {
  featured: boolean,
  logo?: string,
  company?: string,
  new?: boolean,
  position?: string,
  postedAt?: string,
  contract?: string,
  location?: string,
  role?: string,
  level?: string,
  tools?: string[],
  languages?: string[],
}
